//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class DMFTerrority
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual string DmfID
        {
            get { return _dmfID; }
            set
            {
                if (_dmfID != value)
                {
                    if (DMF != null && DMF.DMFID != value)
                    {
                        DMF = null;
                    }
                    _dmfID = value;
                }
            }
        }
        private string _dmfID;
    
        public virtual string CountryName
        {
            get { return _countryName; }
            set
            {
                if (_countryName != value)
                {
                    if (Country != null && Country.CountryName != value)
                    {
                        Country = null;
                    }
                    _countryName = value;
                }
            }
        }
        private string _countryName;
    
        public virtual int State
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (DmfID == null) {
						 error_message.Add(new ErrorMessage("DmfID", "DmfID can not be Null "));
				}
		 if (CountryName == null) {
						 error_message.Add(new ErrorMessage("CountryName", "CountryName can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(DMFTerrority); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual Country Country
        {
            get { return _country; }
            set
            {
                if (!ReferenceEquals(_country, value))
                {
                    var previousValue = _country;
                    _country = value;
                    FixupCountry(previousValue);
                }
            }
        }
        private Country _country;
    
        public virtual DMF DMF
        {
            get { return _dMF; }
            set
            {
                if (!ReferenceEquals(_dMF, value))
                {
                    var previousValue = _dMF;
                    _dMF = value;
                    FixupDMF(previousValue);
                }
            }
        }
        private DMF _dMF;

        #endregion

        #region Association Fixup
    
        private void FixupCountry(Country previousValue)
        {
            if (previousValue != null && previousValue.DMFTerrorities.Contains(this))
            {
                previousValue.DMFTerrorities.Remove(this);
            }
    
            if (Country != null)
            {
                if (!Country.DMFTerrorities.Contains(this))
                {
                    Country.DMFTerrorities.Add(this);
                }
                if (CountryName != Country.CountryName)
                {
                    CountryName = Country.CountryName;
                }
            }
        }
    
        private void FixupDMF(DMF previousValue)
        {
            if (previousValue != null && previousValue.DMFTerrorities.Contains(this))
            {
                previousValue.DMFTerrorities.Remove(this);
            }
    
            if (DMF != null)
            {
                if (!DMF.DMFTerrorities.Contains(this))
                {
                    DMF.DMFTerrorities.Add(this);
                }
                if (DmfID != DMF.DMFID)
                {
                    DmfID = DMF.DMFID;
                }
            }
        }

        #endregion

    }
}
