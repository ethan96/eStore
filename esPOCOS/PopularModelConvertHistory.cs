//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class PopularModelConvertHistory
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual int Id
        {
            get;
            set;
        }
    
        public virtual int ModelLogId
        {
            get { return _modelLogId; }
            set
            {
                if (_modelLogId != value)
                {
                    if (PopularModelLog != null && PopularModelLog.Id != value)
                    {
                        PopularModelLog = null;
                    }
                    _modelLogId = value;
                }
            }
        }
        private int _modelLogId;
    
        public virtual string DocId
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (DocId == null) {
						 error_message.Add(new ErrorMessage("DocId", "DocId can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(PopularModelConvertHistory); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual PopularModelLog PopularModelLog
        {
            get { return _popularModelLog; }
            set
            {
                if (!ReferenceEquals(_popularModelLog, value))
                {
                    var previousValue = _popularModelLog;
                    _popularModelLog = value;
                    FixupPopularModelLog(previousValue);
                }
            }
        }
        private PopularModelLog _popularModelLog;

        #endregion

        #region Association Fixup
    
        private void FixupPopularModelLog(PopularModelLog previousValue)
        {
            if (previousValue != null && previousValue.PopularModelConvertHistories.Contains(this))
            {
                previousValue.PopularModelConvertHistories.Remove(this);
            }
    
            if (PopularModelLog != null)
            {
                if (!PopularModelLog.PopularModelConvertHistories.Contains(this))
                {
                    PopularModelLog.PopularModelConvertHistories.Add(this);
                }
                if (ModelLogId != PopularModelLog.Id)
                {
                    ModelLogId = PopularModelLog.Id;
                }
            }
        }

        #endregion

    }
}
