//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class ProductBoxRule
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual int ID
        {
            get;
            set;
        }
    
        public virtual string StoreID
        {
            get { return _storeID; }
            set
            {
                if (_storeID != value)
                {
                    if (Box != null && Box.StoreID != value)
                    {
                        Box = null;
                    }
                    _storeID = value;
                }
            }
        }
        private string _storeID;
    
        public virtual int BoxID
        {
            get { return _boxID; }
            set
            {
                if (_boxID != value)
                {
                    if (Box != null && Box.BoxID != value)
                    {
                        Box = null;
                    }
                    _boxID = value;
                }
            }
        }
        private int _boxID;
    
        public virtual string PartNoPrefix
        {
            get;
            set;
        }
    
        public virtual bool AdditionalBox
        {
            get;
            set;
        }
    
        public virtual string ExceptionPrefix
        {
            get;
            set;
        }
    
        public virtual string ExceptionSuffix
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (StoreID == null) {
						 error_message.Add(new ErrorMessage("StoreID", "StoreID can not be Null "));
				}
		 if (PartNoPrefix == null) {
						 error_message.Add(new ErrorMessage("PartNoPrefix", "PartNoPrefix can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(ProductBoxRule); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual Box Box
        {
            get { return _box; }
            set
            {
                if (!ReferenceEquals(_box, value))
                {
                    var previousValue = _box;
                    _box = value;
                    FixupBox(previousValue);
                }
            }
        }
        private Box _box;

        #endregion

        #region Association Fixup
    
        private void FixupBox(Box previousValue)
        {
            if (previousValue != null && previousValue.ProductBoxRules.Contains(this))
            {
                previousValue.ProductBoxRules.Remove(this);
            }
    
            if (Box != null)
            {
                if (!Box.ProductBoxRules.Contains(this))
                {
                    Box.ProductBoxRules.Add(this);
                }
                if (BoxID != Box.BoxID)
                {
                    BoxID = Box.BoxID;
                }
                if (StoreID != Box.StoreID)
                {
                    StoreID = Box.StoreID;
                }
            }
        }

        #endregion

    }
}
