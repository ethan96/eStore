//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class ProductWidgetMapping
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual int Id
        {
            get;
            set;
        }
    
        public virtual string StoreID
        {
            get { return _storeID; }
            set
            {
                try
                {
                    _settingFK = true;
                    if (_storeID != value)
                    {
                        if (Product != null && Product.StoreID != value)
                        {
                            var previousValue = _product;
                            _product = null;
                            FixupProduct(previousValue, skipKeys: true);
                        }
                        _storeID = value;
                    }
                }
                finally
                {
                    _settingFK = false;
                }
            }
        }
        private string _storeID;
    
        public virtual string SProductID
        {
            get { return _sProductID; }
            set
            {
                try
                {
                    _settingFK = true;
                    if (_sProductID != value)
                    {
                        if (Product != null && Product.SProductID != value)
                        {
                            var previousValue = _product;
                            _product = null;
                            FixupProduct(previousValue, skipKeys: true);
                        }
                        _sProductID = value;
                    }
                }
                finally
                {
                    _settingFK = false;
                }
            }
        }
        private string _sProductID;
    
        public virtual Nullable<int> WidgetPageID
        {
            get { return _widgetPageID; }
            set
            {
                try
                {
                    _settingFK = true;
                    if (_widgetPageID != value)
                    {
                        if (WidgetPage != null && WidgetPage.WidgetPageID != value)
                        {
                            WidgetPage = null;
                        }
                        _widgetPageID = value;
                    }
                }
                finally
                {
                    _settingFK = false;
                }
            }
        }
        private Nullable<int> _widgetPageID;
    
        public virtual Nullable<System.DateTime> CreateDate
        {
            get;
            set;
        }
    
        public virtual string CreateBy
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   
    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(ProductWidgetMapping); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual Product Product
        {
            get { return _product; }
            set
            {
                if (!ReferenceEquals(_product, value))
                {
                    var previousValue = _product;
                    _product = value;
                    FixupProduct(previousValue);
                }
            }
        }
        private Product _product;
    
        public virtual WidgetPage WidgetPage
        {
            get { return _widgetPage; }
            set
            {
                if (!ReferenceEquals(_widgetPage, value))
                {
                    var previousValue = _widgetPage;
                    _widgetPage = value;
                    FixupWidgetPage(previousValue);
                }
            }
        }
        private WidgetPage _widgetPage;

        #endregion

        #region Association Fixup
    
        private bool _settingFK = false;
    
        private void FixupProduct(Product previousValue, bool skipKeys = false)
        {
            if (Product != null)
            {
                if (StoreID != Product.StoreID)
                {
                    StoreID = Product.StoreID;
                }
                if (SProductID != Product.SProductID)
                {
                    SProductID = Product.SProductID;
                }
            }
            else if (!_settingFK && !skipKeys)
            {
                StoreID = null;
                SProductID = null;
            }
        }
    
        private void FixupWidgetPage(WidgetPage previousValue)
        {
            if (previousValue != null && previousValue.ProductWidgetMappings.Contains(this))
            {
                previousValue.ProductWidgetMappings.Remove(this);
            }
    
            if (WidgetPage != null)
            {
                if (!WidgetPage.ProductWidgetMappings.Contains(this))
                {
                    WidgetPage.ProductWidgetMappings.Add(this);
                }
                if (WidgetPageID != WidgetPage.WidgetPageID)
                {
                    WidgetPageID = WidgetPage.WidgetPageID;
                }
            }
            else if (!_settingFK)
            {
                WidgetPageID = null;
            }
        }

        #endregion

    }
}
