//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class RelatedProduct
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual int RelatedID
        {
            get;
            set;
        }
    
        public virtual string SProductID
        {
            get { return _sProductID; }
            set
            {
                if (_sProductID != value)
                {
                    if (ParentPart != null && ParentPart.SProductID != value)
                    {
                        ParentPart = null;
                    }
                    _sProductID = value;
                }
            }
        }
        private string _sProductID;
    
        public virtual string RelatedSProductID
        {
            get { return _relatedSProductID; }
            set
            {
                if (_relatedSProductID != value)
                {
                    if (RelatedPart != null && RelatedPart.SProductID != value)
                    {
                        RelatedPart = null;
                    }
                    _relatedSProductID = value;
                }
            }
        }
        private string _relatedSProductID;
    
        public virtual string Relationship
        {
            get;
            set;
        }
    
        public virtual string StoreID
        {
            get { return _storeID; }
            set
            {
                if (_storeID != value)
                {
                    if (ParentPart != null && ParentPart.StoreID != value)
                    {
                        ParentPart = null;
                    }
                    if (RelatedPart != null && RelatedPart.StoreID != value)
                    {
                        RelatedPart = null;
                    }
                    _storeID = value;
                }
            }
        }
        private string _storeID;
    
        public virtual System.DateTime CreatedDate
        {
            get;
            set;
        }
    
        public virtual Nullable<bool> IseStoreSetting
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (SProductID == null) {
						 error_message.Add(new ErrorMessage("SProductID", "SProductID can not be Null "));
				}
		 if (RelatedSProductID == null) {
						 error_message.Add(new ErrorMessage("RelatedSProductID", "RelatedSProductID can not be Null "));
				}
		 if (Relationship == null) {
						 error_message.Add(new ErrorMessage("Relationship", "Relationship can not be Null "));
				}
		 if (StoreID == null) {
						 error_message.Add(new ErrorMessage("StoreID", "StoreID can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(RelatedProduct); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual Part ParentPart
        {
            get { return _parentPart; }
            set
            {
                if (!ReferenceEquals(_parentPart, value))
                {
                    var previousValue = _parentPart;
                    _parentPart = value;
                    FixupParentPart(previousValue);
                }
            }
        }
        private Part _parentPart;
    
        public virtual Part RelatedPart
        {
            get { return _relatedPart; }
            set
            {
                if (!ReferenceEquals(_relatedPart, value))
                {
                    var previousValue = _relatedPart;
                    _relatedPart = value;
                    FixupRelatedPart(previousValue);
                }
            }
        }
        private Part _relatedPart;

        #endregion

        #region Association Fixup
    
        private void FixupParentPart(Part previousValue)
        {
            if (previousValue != null && previousValue.RelatedProducts.Contains(this))
            {
                previousValue.RelatedProducts.Remove(this);
            }
    
            if (ParentPart != null)
            {
                if (!ParentPart.RelatedProducts.Contains(this))
                {
                    ParentPart.RelatedProducts.Add(this);
                }
                if (StoreID != ParentPart.StoreID)
                {
                    StoreID = ParentPart.StoreID;
                }
                if (SProductID != ParentPart.SProductID)
                {
                    SProductID = ParentPart.SProductID;
                }
            }
        }
    
        private void FixupRelatedPart(Part previousValue)
        {
            if (previousValue != null && previousValue.RelatedToProducts.Contains(this))
            {
                previousValue.RelatedToProducts.Remove(this);
            }
    
            if (RelatedPart != null)
            {
                if (!RelatedPart.RelatedToProducts.Contains(this))
                {
                    RelatedPart.RelatedToProducts.Add(this);
                }
                if (StoreID != RelatedPart.StoreID)
                {
                    StoreID = RelatedPart.StoreID;
                }
                if (RelatedSProductID != RelatedPart.SProductID)
                {
                    RelatedSProductID = RelatedPart.SProductID;
                }
            }
        }

        #endregion

    }
}
