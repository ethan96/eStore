//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class ScenarioCategory
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual int ScenarioCategoryID
        {
            get;
            set;
        }
    
        public virtual string ScenarioCategoryName
        {
            get;
            set;
        }
    
        public virtual int ScenarioID
        {
            get { return _scenarioID; }
            set
            {
                if (_scenarioID != value)
                {
                    if (Scenario != null && Scenario.CategoryID != value)
                    {
                        Scenario = null;
                    }
                    _scenarioID = value;
                }
            }
        }
        private int _scenarioID;
    
        public virtual string Storeid
        {
            get { return _storeid; }
            set
            {
                if (_storeid != value)
                {
                    if (Scenario != null && Scenario.Storeid != value)
                    {
                        Scenario = null;
                    }
                    if (ProductCategory != null && ProductCategory.Storeid != value)
                    {
                        ProductCategory = null;
                    }
                    _storeid = value;
                }
            }
        }
        private string _storeid;
    
        public virtual int CategoryID
        {
            get { return _categoryID; }
            set
            {
                if (_categoryID != value)
                {
                    if (ProductCategory != null && ProductCategory.CategoryID != value)
                    {
                        ProductCategory = null;
                    }
                    _categoryID = value;
                }
            }
        }
        private int _categoryID;
    
        public virtual int Sequence
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (Storeid == null) {
						 error_message.Add(new ErrorMessage("Storeid", "Storeid can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(ScenarioCategory); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual Application Scenario
        {
            get { return _scenario; }
            set
            {
                if (!ReferenceEquals(_scenario, value))
                {
                    var previousValue = _scenario;
                    _scenario = value;
                    FixupScenario(previousValue);
                }
            }
        }
        private Application _scenario;
    
        public virtual ProductCategory ProductCategory
        {
            get { return _productCategory; }
            set
            {
                if (!ReferenceEquals(_productCategory, value))
                {
                    var previousValue = _productCategory;
                    _productCategory = value;
                    FixupProductCategory(previousValue);
                }
            }
        }
        private ProductCategory _productCategory;
    
        public virtual ICollection<ScenarioProductCategoryMapping> ScenarioProductCategoryMappings
        {
            get
            {
                if (_scenarioProductCategoryMappings == null)
                {
                    var newCollection = new FixupCollection<ScenarioProductCategoryMapping>();
                    newCollection.CollectionChanged += FixupScenarioProductCategoryMappings;
                    _scenarioProductCategoryMappings = newCollection;
                }
                return _scenarioProductCategoryMappings;
            }
            set
            {
                if (!ReferenceEquals(_scenarioProductCategoryMappings, value))
                {
                    var previousValue = _scenarioProductCategoryMappings as FixupCollection<ScenarioProductCategoryMapping>;
                    if (previousValue != null)
                    {
                        previousValue.CollectionChanged -= FixupScenarioProductCategoryMappings;
                    }
                    _scenarioProductCategoryMappings = value;
                    var newValue = value as FixupCollection<ScenarioProductCategoryMapping>;
                    if (newValue != null)
                    {
                        newValue.CollectionChanged += FixupScenarioProductCategoryMappings;
                    }
                }
            }
        }
        private ICollection<ScenarioProductCategoryMapping> _scenarioProductCategoryMappings;

        #endregion

        #region Association Fixup
    
        private void FixupScenario(Application previousValue)
        {
            if (previousValue != null && previousValue.ScenarioCategories.Contains(this))
            {
                previousValue.ScenarioCategories.Remove(this);
            }
    
            if (Scenario != null)
            {
                if (!Scenario.ScenarioCategories.Contains(this))
                {
                    Scenario.ScenarioCategories.Add(this);
                }
                if (Storeid != Scenario.Storeid)
                {
                    Storeid = Scenario.Storeid;
                }
                if (ScenarioID != Scenario.CategoryID)
                {
                    ScenarioID = Scenario.CategoryID;
                }
            }
        }
    
        private void FixupProductCategory(ProductCategory previousValue)
        {
            if (ProductCategory != null)
            {
                if (Storeid != ProductCategory.Storeid)
                {
                    Storeid = ProductCategory.Storeid;
                }
                if (CategoryID != ProductCategory.CategoryID)
                {
                    CategoryID = ProductCategory.CategoryID;
                }
            }
        }
    
        private void FixupScenarioProductCategoryMappings(object sender, NotifyCollectionChangedEventArgs e)
        {
            if (e.NewItems != null)
            {
                foreach (ScenarioProductCategoryMapping item in e.NewItems)
                {
                    item.ScenarioCategory = this;
                }
            }
    
            if (e.OldItems != null)
            {
                foreach (ScenarioProductCategoryMapping item in e.OldItems)
                {
                    if (ReferenceEquals(item.ScenarioCategory, this))
                    {
                        item.ScenarioCategory = null;
                    }
                }
            }
        }

        #endregion

    }
}
