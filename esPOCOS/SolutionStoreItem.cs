//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class SolutionStoreItem
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual string StoreID
        {
            get { return _storeID; }
            set
            {
                if (_storeID != value)
                {
                    if (SolutionStoreTab != null && SolutionStoreTab.StoreID != value)
                    {
                        SolutionStoreTab = null;
                    }
                    _storeID = value;
                }
            }
        }
        private string _storeID;
    
        public virtual int TabID
        {
            get { return _tabID; }
            set
            {
                if (_tabID != value)
                {
                    if (SolutionStoreTab != null && SolutionStoreTab.TabID != value)
                    {
                        SolutionStoreTab = null;
                    }
                    _tabID = value;
                }
            }
        }
        private int _tabID;
    
        public virtual int ItemID
        {
            get;
            set;
        }
    
        public virtual Nullable<int> Sequence
        {
            get;
            set;
        }
    
        public virtual string ItemPicture
        {
            get;
            set;
        }
    
        public virtual string ItemTitle
        {
            get;
            set;
        }
    
        public virtual string ItemDescription
        {
            get;
            set;
        }
    
        public virtual string ItemContent
        {
            get;
            set;
        }
    
        public virtual string ItemLink
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (StoreID == null) {
						 error_message.Add(new ErrorMessage("StoreID", "StoreID can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(SolutionStoreItem); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual SolutionStoreTab SolutionStoreTab
        {
            get { return _solutionStoreTab; }
            set
            {
                if (!ReferenceEquals(_solutionStoreTab, value))
                {
                    var previousValue = _solutionStoreTab;
                    _solutionStoreTab = value;
                    FixupSolutionStoreTab(previousValue);
                }
            }
        }
        private SolutionStoreTab _solutionStoreTab;

        #endregion

        #region Association Fixup
    
        private void FixupSolutionStoreTab(SolutionStoreTab previousValue)
        {
            if (previousValue != null && previousValue.SolutionStoreItems.Contains(this))
            {
                previousValue.SolutionStoreItems.Remove(this);
            }
    
            if (SolutionStoreTab != null)
            {
                if (!SolutionStoreTab.SolutionStoreItems.Contains(this))
                {
                    SolutionStoreTab.SolutionStoreItems.Add(this);
                }
                if (StoreID != SolutionStoreTab.StoreID)
                {
                    StoreID = SolutionStoreTab.StoreID;
                }
                if (TabID != SolutionStoreTab.TabID)
                {
                    TabID = SolutionStoreTab.TabID;
                }
            }
        }

        #endregion

    }
}
