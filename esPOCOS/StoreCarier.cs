//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Reflection;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using eStore.POCOS.PocoX;
 

namespace eStore.POCOS
{
    public partial class StoreCarier
    {
        #region Primitive Properties
     
    	public List<ErrorMessage> error_message{
    		get;
    		set;
    	}
    
    
        public virtual string StoreID
        {
            get { return _storeID; }
            set
            {
                if (_storeID != value)
                {
                    if (Store != null && Store.StoreID != value)
                    {
                        Store = null;
                    }
                    _storeID = value;
                }
            }
        }
        private string _storeID;
    
        public virtual string CarierName
        {
            get { return _carierName; }
            set
            {
                if (_carierName != value)
                {
                    if (ShippingCarier != null && ShippingCarier.CarierName != value)
                    {
                        ShippingCarier = null;
                    }
                    _carierName = value;
                }
            }
        }
        private string _carierName;
    
        public virtual int Priority
        {
            get;
            set;
        }

        #endregion

        #region Validation
    	
    	public bool validate() {
    		error_message = new List<ErrorMessage>();
    	   		 if (StoreID == null) {
						 error_message.Add(new ErrorMessage("StoreID", "StoreID can not be Null "));
				}
		 if (CarierName == null) {
						 error_message.Add(new ErrorMessage("CarierName", "CarierName can not be Null "));
				}

    	   
    	   if(error_message.Count>0) {
    	   	return false;
    	   }else {
    	    return true;
    	   }
    	
    	}
    	
    	/* public void Write()
        {
            Type type = typeof(StoreCarier); // Get type pointer
             PropertyInfo[] fields = type.GetProperties(); // Obtain all fields
                foreach (var pinfo in fields) // Loop through fields
                {
                    string name = pinfo.Name; // Get string name
                    object temp = pinfo.GetValue(pinfo.Name,null); // Get value
                if (temp is int) // See if it is an integer.
                {
                    int value = (int)temp;
                    Console.Write(name);
                    Console.Write(" (int) = ");
                    Console.WriteLine(value);
                }
                else if (temp is string) // See if it is a string.
                {
                    string value = temp as string;
                    Console.Write(name);
                    Console.Write(" (string) = ");
                    Console.WriteLine(value);
                }
            }
        }*/
     
    	
    	
        #endregion

        #region Navigation Properties
    
        public virtual ShippingCarier ShippingCarier
        {
            get { return _shippingCarier; }
            set
            {
                if (!ReferenceEquals(_shippingCarier, value))
                {
                    var previousValue = _shippingCarier;
                    _shippingCarier = value;
                    FixupShippingCarier(previousValue);
                }
            }
        }
        private ShippingCarier _shippingCarier;
    
        public virtual Store Store
        {
            get { return _store; }
            set
            {
                if (!ReferenceEquals(_store, value))
                {
                    var previousValue = _store;
                    _store = value;
                    FixupStore(previousValue);
                }
            }
        }
        private Store _store;

        #endregion

        #region Association Fixup
    
        private void FixupShippingCarier(ShippingCarier previousValue)
        {
            if (previousValue != null && previousValue.StoreCariers.Contains(this))
            {
                previousValue.StoreCariers.Remove(this);
            }
    
            if (ShippingCarier != null)
            {
                if (!ShippingCarier.StoreCariers.Contains(this))
                {
                    ShippingCarier.StoreCariers.Add(this);
                }
                if (CarierName != ShippingCarier.CarierName)
                {
                    CarierName = ShippingCarier.CarierName;
                }
            }
        }
    
        private void FixupStore(Store previousValue)
        {
            if (previousValue != null && previousValue.StoreCariers.Contains(this))
            {
                previousValue.StoreCariers.Remove(this);
            }
    
            if (Store != null)
            {
                if (!Store.StoreCariers.Contains(this))
                {
                    Store.StoreCariers.Add(this);
                }
                if (StoreID != Store.StoreID)
                {
                    StoreID = Store.StoreID;
                }
            }
        }

        #endregion

    }
}
